import styled from '@emotion/styled'

export const SiteContainer = styled.div({
    minWidth: '100%',
    minHeight: '100%',
    borderWidth: .5, 
    borderColor: 'black',
    backgroundColor: 'yellow', 
    margin: 0,
    padding: 0
})  